using UnityEngine;

namespace Old
{
    public class AmmoManager : MonoBehaviour
    {
        public int ammoPlayerCurrentHave; // amount of ammo player have in their inventory
        public int _maxAmmoPlayerCanCarry = 50; //this var can be store in gun data

        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
